# Dockerfile.bash5 — bash 5.x
#
# Alpine 3.18 ships bash 5.2 and Python 3.11 natively — no multi-stage needed.

FROM alpine:3.18
RUN apk add --no-cache bash python3

WORKDIR /shellframe
COPY . .
CMD ["bash", "tests/ptyunit/run.sh"]
